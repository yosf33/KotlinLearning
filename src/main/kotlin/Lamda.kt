fun main() {
    val userId = ""
    print()
}
val myFunction={num1:Int,num2:Int->
    num1+num2
}
//adding data types for the lambda
val myFunctionAddingDataType:(Int,Int)->Int={num1,num2->
    num1+num2
}
val print:()->Unit={
    println("hello lambda")
}
/**
 * write val instead fun
 * use = then{}
 * data type (Int,Int)->Int={num1,num2 ->
 * num1+num2
 * }
 */
